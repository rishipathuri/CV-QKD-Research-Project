# Paper figures (curated)

This folder is for **final, publication-ready figures** that you intend to keep under version control.

Auto-generated plots from `python3 -m cv_qkd_project.main` are written to `outputs/figures/` (ignored). When a figure is “done”, copy it here with a stable filename.

Suggested naming:

- `key_rate_validation.png`
- `robustness_mismatch_asymmetry.png`
